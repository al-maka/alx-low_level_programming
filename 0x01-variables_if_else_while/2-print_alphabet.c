#include <stdio.h>

/**
 * main -  Prints the alphabet in lowercase, followed by a new line.
 *
 * Return: 0
 */
int main(void)
{
	char str = {'a''.''.''b'};
	int a;

	for (a = 0; a < 26)
	{
	putchar("%c\n"), str;
	a++
	}
	return (0)
}
